"""Application entry point.

Initializes and runs the Career Knowledge Extraction pipeline.

This file must stay thin: it should only wire up and run the pipeline,
never contain pipeline logic itself.
"""


def main():
    """Initialize and run the pipeline."""
    raise NotImplementedError  # TODO(Step 2)


if __name__ == "__main__":
    main()

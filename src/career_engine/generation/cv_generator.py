"""Step 3 — Generation.

Renders a Master CV (Markdown) from a CareerProfile. Pure and
deterministic: reads only from the profile, never from the Brain Dump,
and makes no API calls.
"""


def generate_master_cv(profile):
    """Render a CareerProfile into a Master CV as a Markdown string."""
    raise NotImplementedError  # TODO(Step 2)
